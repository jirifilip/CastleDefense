//atribud typ, podle kterého bude vytvářet různé typy přátel
//typ bude číslo, generovat ho bude náhodně generátor nepřátel
define([], function(odkud, pohyb)) {
  function Nepritel() {
    const maxZdravi = 100;
    var aktZdravi = maxZdravi;
    var odkud = odkud;
    var pohyb = pohyb;

  }

  return Nepritel;
}
